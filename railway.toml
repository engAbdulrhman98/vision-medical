[deploy]
startCommand = "php artisan migrate --force; php artisan storage:link 2>/dev/null; php -S 0.0.0.0:$PORT -t public public/router.php"
healthcheckPath = "/up"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
