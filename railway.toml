[deploy]
startCommand = "php artisan migrate --force; php artisan storage:link 2>/dev/null || true; php -S 0.0.0.0:$PORT -t public"
healthcheckPath = "/up"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
